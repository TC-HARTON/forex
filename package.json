{
  "name": "tcharton-fx",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/gen-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/*.test.mjs",
    "check:legal": "node scripts/check-legal.mjs",
    "fetch:calendar": "node scripts/fetch-economic-calendar.mjs",
    "fetch:strength": "node scripts/fetch-currency-strength.mjs",
    "fetch:github": "node scripts/fetch-github-repo.mjs",
    "fetch:all": "npm run fetch:calendar && npm run fetch:strength && npm run fetch:github",
    "verify": "npm run build && npm run check:legal && npm test"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "tailwindcss": "^4.3.0"
  }
}
